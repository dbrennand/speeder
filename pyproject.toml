[project]
name = "speeder"
version = "2.0.0"
description = "Python script to monitor your internet speed! 🚀"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "influxdb-client>=1.49.0",
    "loguru>=0.7.3",
]

[dependency-groups]
dev = [
    "ruff>=0.14.13",
]
